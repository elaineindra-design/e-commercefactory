# Starter requester password. Change this in Vercel before sharing widely.
REQUESTER_PASSWORD=requester123

# Use a long random string in production.
SESSION_SECRET=replace-with-a-long-random-secret

# Shared Supabase storage (recommended).
# SUPABASE_URL is optional because this project already has the URL as a server-side fallback.
SUPABASE_URL=https://clajgzxalhjrtjfccohn.supabase.co
SUPABASE_SECRET_KEY=

# Optional legacy alternative: Upstash Redis REST credentials.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
